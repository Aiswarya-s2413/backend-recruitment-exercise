# AWS / LocalStack
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
LOCALSTACK_ENDPOINT=http://localstack:4566

# DynamoDB / S3
DYNAMODB_TABLE_DOCUMENTS=DocumentsMetadata
METRICS_TABLE=AgentMetrics
S3_BUCKET=pdf-service-bucket

# RAG module (for forwarding)
RAG_MODULE_URL=http://rag_module:8001

# Metrics Lambda URL (dev) - use local stub or LocalStack Lambda invocation
METRICS_LAMBDA_URL=http://metrics_stub:9000/metrics
# Metrics Lambda Name (production) - for direct Lambda invocation via boto3
METRICS_LAMBDA_NAME=AgentMetricsLambda

# App ports
AWS_SERVICE_PORT=8002
